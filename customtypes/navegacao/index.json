{
  "id": "navegacao",
  "label": "Navegação",
  "format": "custom",
  "repeatable": false,
  "status": true,
  "json": {
    "Main": {
      "name": {
        "type": "Text",
        "config": { "label": "Nome", "placeholder": "Nome da lista de menus" }
      },
      "uid": {
        "type": "UID",
        "config": {
          "label": "UID",
          "placeholder": "Id único para o menu de navegação"
        }
      },
      "slices": {
        "type": "Slices",
        "fieldset": "Slice Zone",
        "config": {
          "choices": {
            "menu_opcoes": { "type": "SharedSlice" },
            "nav_contacts": { "type": "SharedSlice" }
          }
        }
      }
    }
  }
}
